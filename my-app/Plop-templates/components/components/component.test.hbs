import {render, screen} from '@testing-library/react';

import {{pascalCase name}} from './{{pascalCase name}}'

test ('render {{pascalCase name}}', () => {
    render (
        <{{pascalCase name}} />);
    
});